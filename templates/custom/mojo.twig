<div id="header">
    {% if not plain %}
    <h1>{{ title }}</h1>

    {% if intro %}
    <p>{{ intro|purify|raw }}</p>
    {% endif %}
    {% endif %}
</div>

<div id="chart"></div>

<div id="footer">
    {% if not plain %}
    <div class="footer-left">
      {% if source %}
          Source:
         {% if sourceURL %}<a target="_blank" class="source" href="{{ sourceURL }}">{% endif %}{{ source$
      {% endif %}
      <a href="data">Get the data</a>
    </div>
    <div class="footer-right">
      <a class="logo" href="http://motherjones.com" target="_blank">
          <img src="{{ ASSET_PATH }}mojo-logo-bw.png" class="small" alt="MOTHER JONES" width="89" height$
      </a>
    </div>
    {% endif %}
</div>